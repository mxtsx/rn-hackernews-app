import { size, space } from "./sizes";
import { fontSize, fontWeight } from "./fonts";
import { colors } from "./colors";

export const theme = {
  size,
  fontSize,
  fontWeight,
  space,
  colors
}
